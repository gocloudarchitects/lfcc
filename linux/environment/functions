greeting () {
  echo "What's your name? " -n
  read input
  echo "Hi, ${input}!"
}

declutter () {
  test $# -eq 1 && grep -vE '^[ ]*#|^[ ]*$' $1
  test $# -ne 1 && echo "Usage: declutter <file>"
}

pingc () {
  ping -C
}
